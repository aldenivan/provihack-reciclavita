const Button = () => {
  return (
    <div>
      <button
        type={"submit"}
        fullWidth
        variant={"contained"}
        color={"primary"}
        onClick={() => {}}
      >
        Saiba como descartar o seu lixo .
      </button>
    </div>
  );
};

export default Button;
